diff --git a/api-server/api/admin/resend-welcome.js b/api-server/api/admin/resend-welcome.js
index 83a1346..b8eba70 100644
--- a/api-server/api/admin/resend-welcome.js
+++ b/api-server/api/admin/resend-welcome.js
@@ -8,8 +8,9 @@ const supabase = createClient(
 module.exports = async function handler(req, res) {
   res.setHeader('Access-Control-Allow-Origin', '*');
   res.setHeader('Access-Control-Allow-Methods', 'POST, GET, OPTIONS');
-  res.setHeader('Access-Control-Allow-Headers', 'Content-Type, Authorization');
+  res.setHeader('Access-Control-Allow-Headers', 'Content-Type, x-internal-secret');
   if (req.method === 'OPTIONS') return res.status(200).end();
+  if (req.headers['x-internal-secret'] !== process.env.INTERNAL_API_SECRET) return res.status(401).json({ error: 'Unauthorized' });
 
   try {
     let sent = 0;
@@ -22,7 +23,7 @@ module.exports = async function handler(req, res) {
         if (cd.email) {
           try {
             await fetch('https://care-voy-api-server.vercel.app/api/notify/send', {
-              method: 'POST', headers: { 'Content-Type': 'application/json' },
+              method: 'POST', headers: { 'Content-Type': 'application/json', 'x-internal-secret': process.env.INTERNAL_API_SECRET },
               body: JSON.stringify({ type: 'welcome', to: cd.email, data: { full_name: cd.full_name, role: 'coordinator' } })
             });
             sent++; results.push(cd.email);
@@ -38,7 +39,7 @@ module.exports = async function handler(req, res) {
         if (st.email) {
           try {
             await fetch('https://care-voy-api-server.vercel.app/api/notify/send', {
-              method: 'POST', headers: { 'Content-Type': 'application/json' },
+              method: 'POST', headers: { 'Content-Type': 'application/json', 'x-internal-secret': process.env.INTERNAL_API_SECRET },
               body: JSON.stringify({ type: 'welcome', to: st.email, data: { full_name: st.full_name, role: 'nemt' } })
             });
             sent++; results.push(st.email);
diff --git a/api-server/api/auth/delete-user.js b/api-server/api/auth/delete-user.js
index 82bbb3e..6836fca 100644
--- a/api-server/api/auth/delete-user.js
+++ b/api-server/api/auth/delete-user.js
@@ -1,9 +1,12 @@
 const { createClient } = require('@supabase/supabase-js');
 
+const SUPABASE_URL = process.env.SUPABASE_URL || 'https://byflpckbjjumxxjxoplk.supabase.co';
+const SUPABASE_ANON_KEY = process.env.SUPABASE_ANON_KEY || 'sb_publishable_z2cTzmjGH3njGM1pGqEV7g_h2ys8C0H';
+
 module.exports = async function handler(req, res) {
   res.setHeader('Access-Control-Allow-Origin', '*');
   res.setHeader('Access-Control-Allow-Methods', 'POST, OPTIONS');
-  res.setHeader('Access-Control-Allow-Headers', 'Content-Type');
+  res.setHeader('Access-Control-Allow-Headers', 'Content-Type, Authorization');
   if (req.method === 'OPTIONS') return res.status(200).end();
   if (req.method !== 'POST') return res.status(405).json({ error: 'Method not allowed' });
 
@@ -11,6 +14,16 @@ module.exports = async function handler(req, res) {
     const { user_id, patient_email } = req.body;
     if (!user_id) return res.status(400).json({ error: 'Missing user_id' });
 
+    // Verify the caller's JWT and ensure they are deleting their own account
+    const authHeader = req.headers['authorization'] || '';
+    const token = authHeader.replace(/^Bearer\s+/i, '');
+    if (!token) return res.status(401).json({ error: 'Unauthorized' });
+
+    const anonClient = createClient(SUPABASE_URL, SUPABASE_ANON_KEY);
+    const { data: { user: caller }, error: authErr } = await anonClient.auth.getUser(token);
+    if (authErr || !caller) return res.status(401).json({ error: 'Invalid or expired token' });
+    if (caller.id !== user_id) return res.status(403).json({ error: 'Forbidden: cannot delete another user' });
+
     const sb = createClient(
       process.env.SUPABASE_URL || 'https://byflpckbjjumxxjxoplk.supabase.co',
       process.env.SUPABASE_SERVICE_ROLE_KEY
diff --git a/api-server/api/chat.js b/api-server/api/chat.js
index 6a0a2f5..a45cab8 100644
--- a/api-server/api/chat.js
+++ b/api-server/api/chat.js
@@ -1,11 +1,22 @@
 const Anthropic = require('@anthropic-ai/sdk');
+const { createClient } = require('@supabase/supabase-js');
+
+const SUPABASE_URL = process.env.SUPABASE_URL || 'https://byflpckbjjumxxjxoplk.supabase.co';
+const SUPABASE_ANON_KEY = process.env.SUPABASE_ANON_KEY || 'sb_publishable_z2cTzmjGH3njGM1pGqEV7g_h2ys8C0H';
+
 module.exports = async function handler(req, res) {
   res.setHeader('Access-Control-Allow-Origin', '*');
   res.setHeader('Access-Control-Allow-Methods', 'POST, OPTIONS');
-  res.setHeader('Access-Control-Allow-Headers', 'Content-Type');
+  res.setHeader('Access-Control-Allow-Headers', 'Content-Type, Authorization');
   if (req.method === 'OPTIONS') return res.status(200).end();
   if (req.method !== 'POST') return res.status(405).json({ error: 'Method not allowed' });
   try {
+    const authHeader = req.headers['authorization'] || '';
+    const token = authHeader.replace(/^Bearer\s+/i, '');
+    if (!token) return res.status(401).json({ error: 'Unauthorized' });
+    const { data: { user }, error: authErr } = await createClient(SUPABASE_URL, SUPABASE_ANON_KEY).auth.getUser(token);
+    if (authErr || !user) return res.status(401).json({ error: 'Invalid or expired token' });
+
     const { message, context } = req.body;
     if (!message) return res.status(400).json({ error: 'Missing message' });
     const system = context === 'patient'
diff --git a/api-server/api/chat/index.js b/api-server/api/chat/index.js
index 0614b4c..a367d3d 100644
--- a/api-server/api/chat/index.js
+++ b/api-server/api/chat/index.js
@@ -1,6 +1,9 @@
 const Anthropic = require("@anthropic-ai/sdk");
 const { createClient } = require("@supabase/supabase-js");
 
+const SUPABASE_URL = process.env.SUPABASE_URL || 'https://byflpckbjjumxxjxoplk.supabase.co';
+const SUPABASE_ANON_KEY = process.env.SUPABASE_ANON_KEY || 'sb_publishable_z2cTzmjGH3njGM1pGqEV7g_h2ys8C0H';
+
 module.exports = async (req, res) => {
   res.setHeader("Access-Control-Allow-Origin", "*");
   res.setHeader("Access-Control-Allow-Methods", "POST, OPTIONS");
@@ -9,10 +12,18 @@ module.exports = async (req, res) => {
   if (req.method !== "POST") return res.status(405).json({ error: "Method not allowed" });
 
   try {
+    const authHeader = req.headers['authorization'] || '';
+    const token = authHeader.replace(/^Bearer\s+/i, '');
+    if (!token) return res.status(401).json({ error: 'Unauthorized' });
+    const { data: { user: caller }, error: authErr } = await createClient(SUPABASE_URL, SUPABASE_ANON_KEY).auth.getUser(token);
+    if (authErr || !caller) return res.status(401).json({ error: 'Invalid or expired token' });
+
     const { messages, patientId, conversationId } = req.body;
+    if (patientId && patientId !== caller.id) return res.status(403).json({ error: 'Forbidden' });
+
     const anthropic = new Anthropic({ apiKey: process.env.ANTHROPIC_API_KEY });
     const supabase = createClient(
-      process.env.SUPABASE_URL,
+      SUPABASE_URL,
       process.env.SUPABASE_SERVICE_ROLE_KEY
     );
 
diff --git a/api-server/api/invite/accept.js b/api-server/api/invite/accept.js
index 97e4159..a5a447e 100644
--- a/api-server/api/invite/accept.js
+++ b/api-server/api/invite/accept.js
@@ -1,7 +1,10 @@
 const { createClient } = require('@supabase/supabase-js');
 
+const SUPABASE_URL = process.env.SUPABASE_URL || 'https://byflpckbjjumxxjxoplk.supabase.co';
+const SUPABASE_ANON_KEY = process.env.SUPABASE_ANON_KEY || 'sb_publishable_z2cTzmjGH3njGM1pGqEV7g_h2ys8C0H';
+
 const supabase = createClient(
-  process.env.SUPABASE_URL,
+  SUPABASE_URL,
   process.env.SUPABASE_SERVICE_ROLE_KEY
 );
 
@@ -13,23 +16,38 @@ module.exports = async function handler(req, res) {
   if (req.method !== 'POST') return res.status(405).json({ error: 'Method not allowed' });
 
   try {
-    const { invite_token, role, uid, full_name, email, password, hospital_id, nemt_partner_id, company_name, city, state, job_title } = req.body;
+    const { invite_token, role, full_name, email, password, hospital_id, nemt_partner_id, company_name, city, state, job_title } = req.body;
     if (!invite_token || !role || !email) return res.status(400).json({ error: 'Missing required fields' });
 
-    let finalUid = uid;
-    if (!finalUid) {
-      const { data: newUser, error: createErr } = await supabase.auth.admin.createUser({ email, password, email_confirm: true });
-      if (createErr) return res.status(400).json({ error: createErr.message });
-      finalUid = newUser.user.id;
-    }
-
+    // FIX 1: Validate invite token BEFORE creating any user account
     const { data: invite, error: inviteErr } = await supabase.from('invites').select('*').eq('token', invite_token).single();
     if (inviteErr || !invite) return res.status(404).json({ error: 'Invite not found' });
     if (invite.used) return res.status(400).json({ error: 'Invite already used' });
-
     const diffDays = (Date.now() - new Date(invite.created_at)) / (1000 * 60 * 60 * 24);
     if (diffDays > 7) return res.status(400).json({ error: 'Invite expired' });
 
+    // FIX 2: Verify the email in the request matches the invite's email
+    if (invite.email && email.toLowerCase() !== invite.email.toLowerCase()) {
+      return res.status(403).json({ error: 'Email does not match this invite' });
+    }
+
+    // FIX 3: Derive uid from verified JWT (existing account) or create a new user — never trust uid from body
+    let finalUid;
+    const authHeader = req.headers['authorization'] || '';
+    const jwtToken = authHeader.replace(/^Bearer\s+/i, '');
+    if (jwtToken) {
+      // Existing Supabase account — verify their JWT and use their real uid
+      const anonClient = createClient(SUPABASE_URL, SUPABASE_ANON_KEY);
+      const { data: { user: caller }, error: authErr } = await anonClient.auth.getUser(jwtToken);
+      if (authErr || !caller) return res.status(401).json({ error: 'Invalid or expired token' });
+      finalUid = caller.id;
+    } else {
+      // New account — create user only after token has been validated above
+      const { data: newUser, error: createErr } = await supabase.auth.admin.createUser({ email, password, email_confirm: true });
+      if (createErr) return res.status(400).json({ error: createErr.message });
+      finalUid = newUser.user.id;
+    }
+
     if (role === 'nemt') {
       let partnerId = nemt_partner_id || invite.nemt_partner_id;
       if (!partnerId && company_name) {
@@ -70,7 +88,7 @@ module.exports = async function handler(req, res) {
     // Send welcome email
     try {
       await fetch('https://care-voy-api-server.vercel.app/api/notify/send', {
-        method: 'POST', headers: { 'Content-Type': 'application/json' },
+        method: 'POST', headers: { 'Content-Type': 'application/json', 'x-internal-secret': process.env.INTERNAL_API_SECRET },
         body: JSON.stringify({ type: 'welcome', to: email, data: { full_name, role } })
       });
     } catch(_) {}
diff --git a/api-server/api/invites/send-sms.js b/api-server/api/invites/send-sms.js
index 64b70e5..7c858b6 100644
--- a/api-server/api/invites/send-sms.js
+++ b/api-server/api/invites/send-sms.js
@@ -8,9 +8,10 @@ const supabase = createClient(
 module.exports = async function handler(req, res) {
   res.setHeader('Access-Control-Allow-Origin', '*');
   res.setHeader('Access-Control-Allow-Methods', 'POST, OPTIONS');
-  res.setHeader('Access-Control-Allow-Headers', 'Content-Type, Authorization');
+  res.setHeader('Access-Control-Allow-Headers', 'Content-Type, x-internal-secret');
   if (req.method === 'OPTIONS') return res.status(200).end();
   if (req.method !== 'POST') return res.status(405).json({ error: 'Method not allowed' });
+  if (req.headers['x-internal-secret'] !== process.env.INTERNAL_API_SECRET) return res.status(401).json({ error: 'Unauthorized' });
 
   try {
     const { phone, patient_name, facility, ride_id } = req.body;
diff --git a/api-server/api/notify/ride-assigned.js b/api-server/api/notify/ride-assigned.js
index 3c2e06c..82112b0 100644
--- a/api-server/api/notify/ride-assigned.js
+++ b/api-server/api/notify/ride-assigned.js
@@ -12,9 +12,10 @@ const supabase = createClient(
 module.exports = async function handler(req, res) {
   res.setHeader('Access-Control-Allow-Origin', '*');
   res.setHeader('Access-Control-Allow-Methods', 'POST, OPTIONS');
-  res.setHeader('Access-Control-Allow-Headers', 'Content-Type');
+  res.setHeader('Access-Control-Allow-Headers', 'Content-Type, x-internal-secret');
   if (req.method === 'OPTIONS') return res.status(200).end();
   if (req.method !== 'POST') return res.status(405).json({ error: 'Method not allowed' });
+  if (req.headers['x-internal-secret'] !== process.env.INTERNAL_API_SECRET) return res.status(401).json({ error: 'Unauthorized' });
 
   try {
     const { channel, phone, email, patient_name, facility, date, ride_id } = req.body || {};
diff --git a/api-server/api/notify/ride-completed.js b/api-server/api/notify/ride-completed.js
index 01603aa..3156bd1 100644
--- a/api-server/api/notify/ride-completed.js
+++ b/api-server/api/notify/ride-completed.js
@@ -1,9 +1,10 @@
 module.exports = async function handler(req, res) {
   res.setHeader('Access-Control-Allow-Origin','*');
   res.setHeader('Access-Control-Allow-Methods','POST,OPTIONS');
-  res.setHeader('Access-Control-Allow-Headers','Content-Type');
+  res.setHeader('Access-Control-Allow-Headers','Content-Type, x-internal-secret');
   if(req.method==='OPTIONS') return res.status(200).end();
   if(req.method!=='POST') return res.status(405).json({error:'Method not allowed'});
+  if(req.headers['x-internal-secret'] !== process.env.INTERNAL_API_SECRET) return res.status(401).json({error:'Unauthorized'});
 
   try {
     const { ride_id, actual_cost } = req.body;
diff --git a/api-server/api/notify/send.js b/api-server/api/notify/send.js
index 3688586..b9a323a 100644
--- a/api-server/api/notify/send.js
+++ b/api-server/api/notify/send.js
@@ -39,9 +39,10 @@ function emailTemplate(title, body, ctaText, ctaUrl) {
 module.exports = async function handler(req, res) {
   res.setHeader('Access-Control-Allow-Origin', '*');
   res.setHeader('Access-Control-Allow-Methods', 'POST, OPTIONS');
-  res.setHeader('Access-Control-Allow-Headers', 'Content-Type, Authorization');
+  res.setHeader('Access-Control-Allow-Headers', 'Content-Type, x-internal-secret');
   if (req.method === 'OPTIONS') return res.status(200).end();
   if (req.method !== 'POST') return res.status(405).json({ error: 'Method not allowed' });
+  if (req.headers['x-internal-secret'] !== process.env.INTERNAL_API_SECRET) return res.status(401).json({ error: 'Unauthorized' });
 
   try {
     const { type, to, data } = req.body;
diff --git a/api-server/api/receipts/export-json.js b/api-server/api/receipts/export-json.js
index bbc1c00..1225bbe 100644
--- a/api-server/api/receipts/export-json.js
+++ b/api-server/api/receipts/export-json.js
@@ -1,5 +1,8 @@
 const { createClient } = require("@supabase/supabase-js");
 
+const SUPABASE_URL = process.env.SUPABASE_URL || 'https://byflpckbjjumxxjxoplk.supabase.co';
+const SUPABASE_ANON_KEY = process.env.SUPABASE_ANON_KEY || 'sb_publishable_z2cTzmjGH3njGM1pGqEV7g_h2ys8C0H';
+
 module.exports = async (req, res) => {
   res.setHeader("Access-Control-Allow-Origin", "*");
   res.setHeader("Access-Control-Allow-Methods", "GET, OPTIONS");
@@ -8,8 +11,17 @@ module.exports = async (req, res) => {
   if (req.method !== "GET") return res.status(405).json({ error: "Method not allowed" });
 
   try {
+    // Verify caller identity before returning PHI
+    const authHeader = req.headers['authorization'] || '';
+    const token = authHeader.replace(/^Bearer\s+/i, '');
+    if (!token) return res.status(401).json({ error: 'Unauthorized' });
+
+    const anonClient = createClient(SUPABASE_URL, SUPABASE_ANON_KEY);
+    const { data: { user: caller }, error: authErr } = await anonClient.auth.getUser(token);
+    if (authErr || !caller) return res.status(401).json({ error: 'Invalid or expired token' });
+
     const supabase = createClient(
-      process.env.SUPABASE_URL,
+      SUPABASE_URL,
       process.env.SUPABASE_SERVICE_ROLE_KEY
     );
     const { rideId } = req.query;
@@ -23,6 +35,13 @@ module.exports = async (req, res) => {
 
     if (!ride) return res.status(404).json({ error: "Ride not found" });
 
+    // Allow access only to the ride's patient or any staff member
+    const isOwner = ride.patient_id === caller.id;
+    if (!isOwner) {
+      const { data: staffRow } = await supabase.from('staff').select('role').eq('id', caller.id).single();
+      if (!staffRow) return res.status(403).json({ error: 'Forbidden' });
+    }
+
     const { data: receipt } = await supabase
       .from("receipts")
       .select("*")
diff --git a/api-server/api/receipts/export-pdf.js b/api-server/api/receipts/export-pdf.js
index b5dc9be..dbb8ef6 100644
--- a/api-server/api/receipts/export-pdf.js
+++ b/api-server/api/receipts/export-pdf.js
@@ -1,5 +1,8 @@
 const { createClient } = require("@supabase/supabase-js");
 
+const SUPABASE_URL = process.env.SUPABASE_URL || 'https://byflpckbjjumxxjxoplk.supabase.co';
+const SUPABASE_ANON_KEY = process.env.SUPABASE_ANON_KEY || 'sb_publishable_z2cTzmjGH3njGM1pGqEV7g_h2ys8C0H';
+
 function fmtDate(iso) {
   if (!iso) return "—";
   return new Date(iso).toLocaleDateString("en-US", { month: "long", day: "numeric", year: "numeric" });
@@ -33,8 +36,16 @@ module.exports = async (req, res) => {
   if (req.method !== "GET") return res.status(405).json({ error: "Method not allowed" });
 
   try {
+    // Verify caller identity before returning PHI
+    const authHeader = req.headers['authorization'] || '';
+    const token = authHeader.replace(/^Bearer\s+/i, '');
+    if (!token) return res.status(401).json({ error: 'Unauthorized' });
+    const anonClient = createClient(SUPABASE_URL, SUPABASE_ANON_KEY);
+    const { data: { user: caller }, error: authErr } = await anonClient.auth.getUser(token);
+    if (authErr || !caller) return res.status(401).json({ error: 'Invalid or expired token' });
+
     const supabase = createClient(
-      process.env.SUPABASE_URL,
+      SUPABASE_URL,
       process.env.SUPABASE_SERVICE_ROLE_KEY
     );
     const { rideId } = req.query;
@@ -48,6 +59,13 @@ module.exports = async (req, res) => {
 
     if (!ride) return res.status(404).json({ error: "Ride not found" });
 
+    // Allow access only to the ride's patient or any staff member
+    const isOwner = ride.patient_id === caller.id;
+    if (!isOwner) {
+      const { data: staffRow } = await supabase.from('staff').select('role').eq('id', caller.id).single();
+      if (!staffRow) return res.status(403).json({ error: 'Forbidden' });
+    }
+
     const { data: receipt } = await supabase
       .from("receipts")
       .select("receipt_number, date")
diff --git a/api-server/api/receipts/generate-pdf.js b/api-server/api/receipts/generate-pdf.js
index 5b2ed1b..770a5fc 100644
--- a/api-server/api/receipts/generate-pdf.js
+++ b/api-server/api/receipts/generate-pdf.js
@@ -8,9 +8,10 @@ const SIGNED_URL_TTL = 60 * 60; // 1 hour — coordinator/admin uses it immediat
 module.exports = async function handler(req, res) {
   res.setHeader('Access-Control-Allow-Origin', '*');
   res.setHeader('Access-Control-Allow-Methods', 'GET, OPTIONS');
-  res.setHeader('Access-Control-Allow-Headers', 'Content-Type, Authorization');
+  res.setHeader('Access-Control-Allow-Headers', 'Content-Type, x-internal-secret');
   if (req.method === 'OPTIONS') return res.status(200).end();
   if (req.method !== 'GET') return res.status(405).json({ error: 'Method not allowed' });
+  if (req.headers['x-internal-secret'] !== process.env.INTERNAL_API_SECRET) return res.status(401).json({ error: 'Unauthorized' });
 
   const { rideId } = req.query;
   if (!rideId) return res.status(400).json({ error: 'rideId required' });
diff --git a/api-server/api/receipts/generate.js b/api-server/api/receipts/generate.js
index 6408ea6..75e3107 100644
--- a/api-server/api/receipts/generate.js
+++ b/api-server/api/receipts/generate.js
@@ -46,9 +46,10 @@ function fmtTimestamp(iso) {
 module.exports = async (req, res) => {
   res.setHeader("Access-Control-Allow-Origin", "*");
   res.setHeader("Access-Control-Allow-Methods", "POST, OPTIONS");
-  res.setHeader("Access-Control-Allow-Headers", "Content-Type, Authorization");
+  res.setHeader("Access-Control-Allow-Headers", "Content-Type, x-internal-secret");
   if (req.method === "OPTIONS") return res.status(200).end();
   if (req.method !== "POST") return res.status(405).json({ error: "Method not allowed" });
+  if (req.headers["x-internal-secret"] !== process.env.INTERNAL_API_SECRET) return res.status(401).json({ error: "Unauthorized" });
 
   try {
     const supabase = createClient(
diff --git a/api-server/api/reminders/send.js b/api-server/api/reminders/send.js
index 1f5fdfe..b9c0cca 100644
--- a/api-server/api/reminders/send.js
+++ b/api-server/api/reminders/send.js
@@ -13,6 +13,15 @@ module.exports = async function handler(req, res) {
   if (req.method !== 'POST') return res.status(405).json({ error: 'Method not allowed' });
 
   try {
+    const authHeader = req.headers['authorization'] || '';
+    const jwtToken = authHeader.replace(/^Bearer\s+/i, '');
+    if (!jwtToken) return res.status(401).json({ error: 'Unauthorized' });
+    const anonSb = createClient(process.env.SUPABASE_URL, process.env.SUPABASE_ANON_KEY || 'sb_publishable_z2cTzmjGH3njGM1pGqEV7g_h2ys8C0H');
+    const { data: { user: caller }, error: authErr } = await anonSb.auth.getUser(jwtToken);
+    if (authErr || !caller) return res.status(401).json({ error: 'Invalid or expired token' });
+    const { data: callerStaff } = await supabase.from('staff').select('role').eq('id', caller.id).single();
+    if (!callerStaff || !['admin', 'coordinator'].includes(callerStaff.role)) return res.status(403).json({ error: 'Forbidden' });
+
     const { ride_id } = req.body;
     if (!ride_id) return res.status(400).json({ error: 'Missing ride_id' });
 
diff --git a/api-server/api/rides/assign.js b/api-server/api/rides/assign.js
index 7b960d3..834920e 100644
--- a/api-server/api/rides/assign.js
+++ b/api-server/api/rides/assign.js
@@ -43,7 +43,7 @@ module.exports = async function handler(req, res) {
           for (const coord of coords) {
             if (coord.email) {
               await fetch('https://care-voy-api-server.vercel.app/api/notify/send', {
-                method: 'POST', headers: { 'Content-Type': 'application/json' },
+                method: 'POST', headers: { 'Content-Type': 'application/json', 'x-internal-secret': process.env.INTERNAL_API_SECRET },
                 body: JSON.stringify({ type: 'ride_confirmed', to: coord.email, data: { patient_name: rideFull.patient_name, date: dateStr } })
               });
             }
diff --git a/api-server/api/rides/update-status.js b/api-server/api/rides/update-status.js
index d6693f1..b8ee8e0 100644
--- a/api-server/api/rides/update-status.js
+++ b/api-server/api/rides/update-status.js
@@ -3,11 +3,21 @@ const { createClient } = require('@supabase/supabase-js');
 module.exports = async function handler(req, res) {
   res.setHeader('Access-Control-Allow-Origin', '*');
   res.setHeader('Access-Control-Allow-Methods', 'POST, OPTIONS');
-  res.setHeader('Access-Control-Allow-Headers', 'Content-Type');
+  res.setHeader('Access-Control-Allow-Headers', 'Content-Type, Authorization');
   if (req.method === 'OPTIONS') return res.status(200).end();
   if (req.method !== 'POST') return res.status(405).json({ error: 'Method not allowed' });
 
   try {
+    const authHeader = req.headers['authorization'] || '';
+    const jwtToken = authHeader.replace(/^Bearer\s+/i, '');
+    if (!jwtToken) return res.status(401).json({ error: 'Unauthorized' });
+    const anonSb = createClient(
+      process.env.SUPABASE_URL || 'https://byflpckbjjumxxjxoplk.supabase.co',
+      process.env.SUPABASE_ANON_KEY || 'sb_publishable_z2cTzmjGH3njGM1pGqEV7g_h2ys8C0H'
+    );
+    const { data: { user: caller }, error: authErr } = await anonSb.auth.getUser(jwtToken);
+    if (authErr || !caller) return res.status(401).json({ error: 'Invalid or expired token' });
+
     const { ride_id, status, driver_name, driver_phone, action } = req.body;
     if (!ride_id) return res.status(400).json({ error: 'Missing ride_id' });
 
@@ -15,9 +25,12 @@ module.exports = async function handler(req, res) {
       process.env.SUPABASE_URL || 'https://byflpckbjjumxxjxoplk.supabase.co',
       process.env.SUPABASE_SERVICE_ROLE_KEY
     );
+    const { data: callerStaff } = await sb.from('staff').select('role').eq('id', caller.id).single();
+    if (!callerStaff || !['admin', 'coordinator', 'nemt'].includes(callerStaff.role)) return res.status(403).json({ error: 'Forbidden' });
 
-    // Handle ride deletion
+    // Handle ride deletion — admin only
     if (action === 'delete') {
+      if (callerStaff.role !== 'admin') return res.status(403).json({ error: 'Forbidden: only admins can delete rides' });
       const { error } = await sb.from('rides').delete().eq('id', ride_id);
       if (error) return res.status(500).json({ error: error.message });
       return res.status(200).json({ success: true, action: 'deleted' });
diff --git a/api-server/api/staff/lookup.js b/api-server/api/staff/lookup.js
index ff7fd56..0f38b95 100644
--- a/api-server/api/staff/lookup.js
+++ b/api-server/api/staff/lookup.js
@@ -3,9 +3,10 @@ const { createClient } = require('@supabase/supabase-js');
 module.exports = async function handler(req, res) {
   res.setHeader('Access-Control-Allow-Origin', '*');
   res.setHeader('Access-Control-Allow-Methods', 'POST, OPTIONS');
-  res.setHeader('Access-Control-Allow-Headers', 'Content-Type');
+  res.setHeader('Access-Control-Allow-Headers', 'Content-Type, x-internal-secret');
   if (req.method === 'OPTIONS') return res.status(200).end();
   if (req.method !== 'POST') return res.status(405).json({ error: 'Method not allowed' });
+  if (req.headers['x-internal-secret'] !== process.env.INTERNAL_API_SECRET) return res.status(401).json({ error: 'Unauthorized' });
 
   try {
     const { uid, nemt_partner_id } = req.body;
diff --git a/partners-portal/admin.html b/partners-portal/admin.html
index 9345a35..dd24138 100644
--- a/partners-portal/admin.html
+++ b/partners-portal/admin.html
@@ -1106,7 +1106,7 @@
   if (!confirm('Delete this ride permanently?')) return;
   try {
     var resp = await fetch('https://care-voy-api-server.vercel.app/api/rides/update-status', {
-      method: 'POST', headers: { 'Content-Type': 'application/json' },
+      method: 'POST', headers: { 'Content-Type': 'application/json', 'Authorization': 'Bearer ' + token },
       body: JSON.stringify({ ride_id: rideId, action: 'delete' })
     });
     var result = await resp.json();
diff --git a/partners-portal/coordinator.html b/partners-portal/coordinator.html
index b574435..18cfbf6 100644
--- a/partners-portal/coordinator.html
+++ b/partners-portal/coordinator.html
@@ -799,7 +799,7 @@ async function deleteRide(evt, rideId) {
 async function resendReceipt(rideId) {
   try {
     var resp = await fetch(API + '/api/rides/update-status', {
-      method: 'POST', headers: { 'Content-Type': 'application/json' },
+      method: 'POST', headers: { ...H, 'Content-Type': 'application/json' },
       body: JSON.stringify({ ride_id: rideId, status: 'completed', resend_only: true })
     });
     showToast('Receipt resent to patient', 'ok');
diff --git a/partners-portal/driver.html b/partners-portal/driver.html
index 0d0edba..14a75cd 100644
--- a/partners-portal/driver.html
+++ b/partners-portal/driver.html
@@ -709,7 +709,7 @@ async function updateStatus(rideId, newStatus) {
   try {
     var resp = await fetch(API + '/api/rides/update-status', {
       method: 'POST',
-      headers: { 'Content-Type': 'application/json' },
+      headers: { ...H, 'Content-Type': 'application/json' },
       body: JSON.stringify({ ride_id: rideId, status: newStatus })
     });
     var result = await resp.json();
diff --git a/partners-portal/patients.html b/partners-portal/patients.html
index 72f9102..6ac2529 100644
--- a/partners-portal/patients.html
+++ b/partners-portal/patients.html
@@ -718,10 +718,11 @@ async function deleteMyAccount() {
     await sb.from('patients').delete().eq('id', currentUser.id);
     // Delete auth user via API (requires service role)
     try {
+      const { data: { session: _delSess } } = await sb.auth.getSession();
       await fetch(API + '/api/auth/delete-user', {
         method: 'POST',
-        headers: { 'Content-Type': 'application/json' },
-        body: JSON.stringify({ user_id: currentUser.id })
+        headers: { 'Content-Type': 'application/json', 'Authorization': 'Bearer ' + (_delSess ? _delSess.access_token : '') },
+        body: JSON.stringify({ user_id: currentUser.id, patient_email: currentUser.email })
       });
     } catch(e2) {}
     await sb.auth.signOut();
@@ -1058,9 +1059,10 @@ async function sendChat() {
   msgs.innerHTML += `<div class="chat-msg ai typing" id="typingMsg">Thinking…</div>`;
   msgs.scrollTop = msgs.scrollHeight;
   try {
+    const { data: { session: _chatSess } } = await sb.auth.getSession();
     const res  = await fetch(API + '/api/chat', {
       method: 'POST',
-      headers: { 'Content-Type': 'application/json' },
+      headers: { 'Content-Type': 'application/json', 'Authorization': 'Bearer ' + (_chatSess ? _chatSess.access_token : '') },
       body: JSON.stringify({ message: msg, context: 'patient' })
     });
     const data = await res.json();
